import { DialogWindowDirective } from './dialog-window.directive';

describe('DialogWindowDirective', () => {
  it('should create an instance', () => {
    const directive = new DialogWindowDirective();
    expect(directive).toBeTruthy();
  });
});
